% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mzid.R
\name{read_dfdr_mzid}
\alias{read_dfdr_mzid}
\title{Read mzIdentML into a dfdr_tbl (generic; score-based TDC q-values)}
\usage{
read_dfdr_mzid(
  mzid_path,
  rank = 1L,
  score_accession_preference = c("MS:1002257", "MS:1001330", "MS:1001328", "MS:1002052",
    "MS:1002049", "MS:1001331", "MS:1001171", "MS:1001950", "MS:1002466"),
  score_direction = c("auto", "lower_better", "higher_better"),
  add_decoy = 1L,
  min_score_coverage = 1,
  decoy_regex = "(^##|_REVERSED$|^REV_|^DECOY_)",
  unit = "psm",
  scope = NA_character_,
  provenance = list()
)
}
\arguments{
\item{mzid_path}{Character scalar. Path to an mzIdentML file (.mzid).}

\item{rank}{Integer scalar. Which \code{SpectrumIdentificationItem@rank} to use. Default 1.}

\item{score_accession_preference}{Character vector. Preferred PSI-MS CV accessions to treat
as the primary PSM score, in priority order.}

\item{score_direction}{One of \code{"auto"}, \code{"lower_better"}, \code{"higher_better"}.
\code{"auto"} uses minimal built-in rules for common accessions.}

\item{add_decoy}{Integer scalar. Additive correction in the TDC FDR estimate:
\eqn{\widehat{FDR} = (D + add\_decoy) / T}. Default 1.}

\item{min_score_coverage}{Numeric in (0,1]. Minimum fraction of PSMs required to have
the chosen score CV term. Default 1.0 (strict; no missing scores allowed).}

\item{decoy_regex}{Character scalar. Regex used to infer decoys from protein accessions
if \code{PeptideEvidence@isDecoy} is not informative. Default catches common patterns.}

\item{unit}{Character. Stored in dfdr_tbl metadata. Default \code{"psm"}.}

\item{scope}{Character. Stored in dfdr_tbl metadata. Default \code{NA_character_}.}

\item{provenance}{Named list. Stored in dfdr_tbl metadata (tool, version, params, command, ...).}
}
\value{
A \code{dfdr_tbl}.
}
\description{
Extracts a competed PSM universe (rank-1 by default) from an mzIdentML file,
determines target/decoy labels, selects a single numeric PSM score CV term,
and reconstructs q-values using target-decoy counting (TDC).
}
\details{
This is designed for workflows where mzIdentML does not contain explicit q-values or PEPs.
}
