% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_dfdr_tbl.R
\name{print.dfdr_tbl}
\alias{print.dfdr_tbl}
\title{Print a \code{dfdr_tbl}}
\usage{
\method{print}{dfdr_tbl}(x, ...)
}
\arguments{
\item{x}{A \code{dfdr_tbl}.}

\item{...}{Passed to the next print method.}
}
\value{
Returns \code{x} invisibly (called for its printing side effect).
}
\description{
Prints a compact header with attached metadata (unit, scope, q-source, etc.)
and then falls back to the default tibble/data.frame print method.
}
\examples{
library(tibble)

df <- tibble(
  id = as.character(1:3),
  run = "run1",
  is_decoy = c(FALSE, TRUE, FALSE),
  score = c(10, 9, 8),
  q = c(0.01, 0.02, 0.03),
  pep = c(0.01, NA, 0.03)
)
x <- as_dfdr_tbl(df, unit = "psm", scope = "global", q_source = "toy_q")
x

}
